<%= form_for([@commentable, comment]) do |f| %>
  <% if policy(comment).approve? && !comment.approved? %>
    <%# TODO: modify form to have a submit that changes approved boolean to ture %>
    <%# f.label :approved, class: "right inline", title: "Is this comment approved?", data: {tooltip: true } %>
    <%# f.check_box :approved, class: "right inline", title: "Is this comment approved?",
            data: {tooltip: true } %>
    <%# THIS IS AWESOME %>
    <%= f.hidden_field :approved, value: true %>
    <%= f.submit "Approve" %>
  <% end %>
<% end %>
